ARG DENO_VERSION=2.4.0
FROM app/deno-base:${DENO_VERSION}

# Copy application code with proper ownership
USER root
COPY --chown=1000:1000 . .
USER 1000:1000

CMD ["sh", "-c", "deno task db:migrate && deno task api:dev"]
